spring:
  ai:
    mcp:
      server:
        stdio: false
        name: webmvc-mcp-server
        version: 1.0.0
        type: SYNC
        capabilities:
          tool: true
          resource: true
          prompt: true
          completion: true
  config:
    import:
      - classpath:ds.yml

server:
  port: 8090